rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Enable full read/write access for internal backend or app use
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
